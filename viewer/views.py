# -*- coding: utf-8 -*-
from __future__ import unicode_literals

from django.shortcuts import render

# Create your views here.
def begin_registration(
        request, template, form_class, extra_context=None, when=None,
        *args, **kwargs):
        pass
